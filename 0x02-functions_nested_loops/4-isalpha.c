#include "main.h"

/**
 * _isalpha - checks if its alphabet
 * @c: argument that passes into function
 * Return: always 0
 **/

int _isalpha(int c)
{
	if (_isalpha(c) == 0)
		return (0);
	else
		return (1);
}
