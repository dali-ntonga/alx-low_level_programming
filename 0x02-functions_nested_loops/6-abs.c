#include "main.h"

/**
 * _abs - calculates the absolute value
 * @c: argument to be passed into function
 * Return: always 0
 **/

int _abs(int c)
{
	if (c > 0 || C == 0)
	{
		return (c);
	}
	else
		return (c * -1);
}

